# CI context

codex-barbar CI runs entirely on official GitHub-hosted runners; there is no
shared third-party pool, budget variable, or spend gate. See `.github/CI.md`
for the exact workflows and commands.

This file exists so historical references to the old Blacksmith pool and
`CI_BUDGET_MODE` keep a single, current answer: they are gone.
